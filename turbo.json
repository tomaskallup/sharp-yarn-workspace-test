{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "build/**",
        "dist/**",
        "tsconfig.build.tsbuildinfo"
      ]
    },
    "gcp-build": {
      "cache": false
    }
  }
}
